# 引き継ぎ — 2026-08-04 深夜時点

**Phase 1a 期限: 2026-08-08（あと4日）**

スマホで読む用の要約。**Cloud Agent 向け正式版:** [`docs/handoffs/active/local-to-cloud-grafana-alert-esp32-20260804.md`](../handoffs/active/local-to-cloud-grafana-alert-esp32-20260804.md)（要 **git push**）

次セッションは `docs/handoffs/INDEX.md` → 上記 → [`cloud-agent-handoff-2026-08.md`](./cloud-agent-handoff-2026-08.md) の順。

---

## 今日完了 ✅

| 項目 | 状態 |
|------|------|
| GCP サービスアカウント | `grafana-bigquery@aquapulse-dev.iam.gserviceaccount.com` |
| Cloud Resource Manager API | 有効化済み |
| Grafana Cloud アカウント | `microteal1944`（Free + 14日 Pro trial） |
| BigQuery データソース | **Save & test 成功** |
| Explore テスト | `mac_poller_v1` データ確認 OK |
| ダッシュボード | **Import 成功（v3）** — スマホ向けレイアウト |
| Mac Tapo poller | **稼働中**（15分 cron、最新データ OK） |

---

## いま動いているもの

```
Tapo H100+T310 / P300 (192.168.10.x)
    ↓ 15分ごと cron
Mac: scripts/run-tapo-poller.sh
    ↓ HTTPS POST
GCP ingest → BigQuery aquapulse.sensor_readings
    ↓
Grafana Cloud（BigQuery datasource）
    ↓
ダッシュボード「AquaPulse Mac Poller」（スマホで閲覧可）
```

**最新値（2026-08-04 00:30 JST 頃）:** 室温 ~25.1°C / 湿度 ~66% / ファン ON / 照明 OFF

**確認コマンド（Mac）:**
```bash
./scripts/check-poller-data.sh 3
```

---

## Grafana 関連ファイル

| 用途 | パス |
|------|------|
| ダッシュボード JSON（v3） | `grafana/dashboards/aquapulse-mac-poller-bigquery.json` |
| Desktop コピー | `~/Desktop/aquapulse-mac-poller-bigquery.json` |
| SA キー（**git 禁止**） | `~/grafana-bigquery-key.json` |
| セットアップ手順 | `docs/operations/grafana-bigquery-setup-2026-08-03.md` |

**再 Import:** Dashboards → Import → JSON 選択 → datasource 選び直し

**v3 で直したバグ:**
- BigQuery では `AS "室温"` が構文エラー → ASCII エイリアスに変更
- State timeline は `CAST(ROUND(value) AS INT64)` 必須
- 時間フィルタは `TIMESTAMP_MILLIS($__from/$__to)`

---

## ネットワーク（触らない）

| 項目 | 値 |
|------|-----|
| WiFi | `aterm-b88a47-2s`（2.4GHz） |
| H100 Hub | `192.168.10.110` |
| P300 | `192.168.10.104` |
| ルーター | mesh OFF、`-2s` ネットワーク分離 OFF |
| Tapo アプリ | 音声アシスタント → サードパーティ連携 **ON** |

詳細ログ: [`docs/operations/tapo-poller-mac-setup-log-2026-08-03.md`](../operations/tapo-poller-mac-setup-log-2026-08-03.md)

---

## 未コミット（ローカルのみ）

```
?? docs/operations/grafana-bigquery-setup-2026-08-03.md
?? docs/guides/handoff-2026-08-04.md          ← このファイル
?? grafana/dashboards/aquapulse-mac-poller-bigquery.json
?? scripts/check-poller-data.sh
```

コミットはユーザー指示時のみ。

---

## 次にやること（優先順）

### P1 — Phase 1a 必須（〜8/8）

1. **Grafana アラート** — 室温 ≥ 28°C → メール通知
   - Alerts & IRM → Alert rules → New
   - Query: 直近5分の `room_temperature` MAX
   - Condition: > 28
   - Contact point: Email 設定
2. **ESP32 + DS18B20** — 水槽水温センサー
   - ハード在庫確認（ESP32、DS18B20、抵抗、ジャンパ線）
   - WiFi 接続 → `ingest` POST
   - BigQuery に `device_id=esp32_001` 等でデータ確認
3. **ダッシュボード拡張** — 水温パネル追加（ESP32 データ来たら）
4. **3日連続監視テスト** — poller + ESP32 の uptime 99%+

### P2 — あると良い

- Secret Manager `tapo-p300-ip` を `.104` に更新（今 `.101` の可能性）
- ルーター DHCP 固定（`.110` / `.104`）
- 未コミットファイルを `main` に push
- Mac スリープ対策の恒久化（`keep-mac-awake.sh` / `pmset`）

### P3 — Phase 1b 以降

- ファン自動制御（VPN / GL.iNet 等）
- ESP32 OTA
- `aquarium-thermostat-complete-manual.md` の Phase 1a 向け更新

---

## Phase 1a の運用フロー（確定）

```
水温/室温が Grafana で見える
    ↓
28°C 以上でアラート（メール）
    ↓
ユーザーが Tapo アプリでファン ON（手動）
    ↓
26°C 以下になったらファン OFF（手動）
```

**自動ファン制御は Phase 1a スコープ外**（ADR-0008）。

---

## 既知の注意点

| 症状 | 対処 |
|------|------|
| poller 止まる | Mac スリップ / 蓋閉じ → `keep-mac-awake.sh` |
| P300 接続失敗 | サードパーティ連携 OFF→10s→ON、同一 SSID 確認 |
| Grafana No data | JSON v3 再 Import、SQL エイリアス確認 |
| GCP から Tapo 不可 | 設計通り。Mac poller か ESP32 のみ |

---

## 次セッション用プロンプト（コピペ）

```
AquaPulse Phase 1a。まず docs/guides/handoff-2026-08-04.md を読む。

LIVE:
- Mac poller → BigQuery OK（Hub .110, P300 .104）
- Grafana Cloud + BigQuery datasource OK
- ダッシュボード v3 Import 済み（スマホ UI）

TODO 次:
1. Grafana アラート ≥28°C メール
2. ESP32 + DS18B20 配線・ingest POST
3. 未コミット 4 ファイルの commit/push（ユーザー指示時）

期限 2026-08-08。日本語。GCP deploy/commit は指示があるまでしない。
Tapo LAN 再トラブルシュートは poller が壊れるまで不要。
```

---

## 参照リンク

| 名称 | URL / パス |
|------|------------|
| ingest | `https://ingest-e4jnfqozuq-an.a.run.app` |
| GCP プロジェクト | `aquapulse-dev` |
| BigQuery テーブル | `aquapulse-dev.aquapulse.sensor_readings` |
| Grafana Cloud | ログイン後 Stack URL（`*.grafana.net`） |
| マスター handoff | `docs/guides/cloud-agent-handoff-2026-08.md` |

---

**最終更新:** 2026-08-04 00:45 JST  
**セッション:** Grafana E2E 完了 → 次はアラート + ESP32
